# ── Build artefacts ────────────────────────────────────────────────────────
target/
node_modules/
dist/
build/

# ── Secrets / signing material (NEVER commit) ──────────────────────────────
.env
.env.local
.env.*.local
*.key
*.pfx
*.p12
*.cer
*.cert
*.crt
*.keystore
*.jks

# ── IDE / editor ───────────────────────────────────────────────────────────
.vs/
.vscode/
.idea/
*.suo
*.user

# ── OS junk ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ───────────────────────────────────────────────────────────────────
*.log

# ── Claude Code tooling / harness state (keep local, not in public repo) ────
.claude/

# ── Internal docs — keep local, not in the public repo ─────────────────────
CLAUDE.md
TRIGR_TAURI_CONTEXT.md
TRIGR_CONTEXT_ELECTRON_ARCHIVE.md
email-signature.html

# ── Internal planning docs ─────────────────────────────────────────────────
docs/private/
docs/windows-smartscreen.mp4

# ── Real competitor-tool exports for importer testing (personal content) ────
test-fixtures/importers/private/

# ── Local release-asset stash (for VT scans + local testing) ───────────────
.local-releases/
