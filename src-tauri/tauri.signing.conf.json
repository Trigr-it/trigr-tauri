{
  "bundle": {
    "windows": {
      "signCommand": "pwsh -NoProfile -Command \"if(-not(Test-Path -LiteralPath '%1')){Write-Output('[sign-skip] '+'%1');exit 0};Write-Output('[sign] '+'%1');& trusted-signing-cli -e https://weu.codesigning.azure.net -a nodescaffold-signing -c node-public-trust -d Trigr '%1';exit $LASTEXITCODE\""
    }
  }
}
